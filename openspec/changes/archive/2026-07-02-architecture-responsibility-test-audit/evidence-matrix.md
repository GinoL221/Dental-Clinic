# Evidence Matrix

| ID | Area | Path | Symbol/Concern | Rule/Principle | Evidence | Classification | Confidence | Test Coverage | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BM-01 | Backend | `backend/src/main/java/com/dh/dentalClinicMVC/authentication/AuthenticationController.java` | `checkEmailExists` injects `IUserRepository` directly | Controller → Service interface → Repository; keep controllers thin | Controller imports `IUserRepository`, stores it as a field, and calls `userRepository.findByEmail(email).isPresent()` inside the controller | violation | high | No explicit boundary test found for this endpoint | Clear controller-to-repository bypass; the only existing auth controller test checks registration behavior, not this route |
| BM-02 | Backend | `backend/src/main/java/com/dh/dentalClinicMVC/controller/PatientController.java` | `save(@Valid @RequestBody Patient patient)` / `update(@RequestBody Patient patient, Authentication auth)` | DTO boundary; `@Valid`; keep controllers from owning entity-shape request contracts | Save/update accept entity bodies; update mutates `id`, `role`, and `email` in-controller before delegating | partial | high | Authz tests cover ownership/forgery scenarios, but not request-shape hardening | Response uses DTO, but request side remains entity-shaped and controller-owned |
| BM-03 | Backend | `backend/src/main/java/com/dh/dentalClinicMVC/controller/DentistController.java` | `save(@Valid @RequestBody Dentist dentist)` / `update(@RequestBody Dentist dentist, Authentication auth)` | DTO boundary; `@Valid`; constructor injection preference | Same pattern as patient controller: entity request body, in-controller identity/role sanitization, service call after mutation | partial | high | Authz tests exist for ownership/forgery, not request DTO shape | Response uses DTO, request path still leaks entity semantics |
| BM-04 | Backend | `backend/src/main/java/com/dh/dentalClinicMVC/controller/AppointmentController.java` | Missing `@Valid` on request bodies; manual status parsing and existence checks | `@Valid`; move orchestration/validation to service boundary where practical | `save`, `update`, and `updateStatus` use raw `@RequestBody` payloads and manual `IllegalArgumentException` checks | partial | medium | Controller integration tests cover behavior, not explicit validation contract | DTO boundary is present, but controller still owns too much validation/orchestration |
| BM-05 | Backend | `backend/src/main/java/com/dh/dentalClinicMVC/controller/SpecialtyController.java` | Service + DTO + validation + security wiring | Controller → Service boundary; DTO/API contract; `@Valid`; `@PreAuthorize` | Uses `ISpecialtyService`, DTOs, `@Valid`, and role annotations consistently | aligned | high | Covered by `SpecialtyControllerTest` | Positive example to preserve |
| BM-06 | Backend | `backend/src/main/java/com/dh/dentalClinicMVC/exception/GlobalExceptionHandler.java` | Centralized exception mapping | Exception handling must go through `GlobalExceptionHandler` | Handles not-found, bad credentials, invalid args, forbidden, duplicate, and generic exceptions centrally | aligned | high | Exception behavior indirectly covered by controller/integration tests | Strong project-rule fit |
| BM-07 | Backend | `backend/src/main/java/com/dh/dentalClinicMVC/service/impl/DashboardServiceImpl.java` | `@Autowired` on constructor | Prefer constructor injection; avoid field injection | Constructor injection is used; annotation is redundant rather than a field-injection leak | aligned | medium | No dedicated injection-style test needed | Minor cleanliness issue only |
| FM-01 | Frontend | `frontend/app.js` | Express composition root | Keep routes thin; push logic to controllers/middlewares | Wires env, sessions, user-data middleware, static assets, routes, and 404 only | aligned | high | Route protection covered separately | Good entrypoint structure |
| FM-02 | Frontend | `frontend/src/routes/routes.js` | Route delegation | Express routes should stay thin | Top-level routes delegate to subrouters; protected sections use `requireAuth` | aligned | high | `require-auth.test.js` covers protection behavior | Good thin-route baseline |
| FM-03 | Frontend | `frontend/src/routes/dashboardRoutes.js` | Inline auth/role check before render | Keep route responsibility thin; avoid business logic in routes | Route checks `req.session.user` and `role === 'ADMIN'` inline before rendering dashboard | partial | medium | No dedicated dashboard route behavior test found | Access policy is correct, but route owns presentation-policy branching |
| FM-04 | Frontend | `frontend/src/middlewares/userDataMiddleware.js` | `res.locals` bootstrap and session cleanup script | Middleware responsibility; avoid business logic in views | Populates `res.locals.user`, role flags, and logout cleanup script for templates | aligned | medium | Covered indirectly by view/route tests | Acceptable session-plumbing middleware |
| FM-05 | Frontend | `frontend/src/views/dashboard/dashboard.ejs` | `window.serverData`, `window.currentUser`, `window.isAdmin` | EJS should remain presentational; global bootstrap must be justified | Inline script injects user/role globals and performs date-setting DOM logic | partial | medium | Source-contract tests cover globals; no runtime behavior test found | Mostly bootstrap data, but mixes rendering and behavior |
| FM-06 | Frontend | `frontend/src/views/appointments/appointmentAdd.ejs` / `appointmentEdit.ejs` / `appointmentList.ejs` | `window.serverData` and `window.currentUser`/`window.isAdmin` globals; inline DOM logic | EJS presentational boundary; global-state leakage | Templates publish shared state to `window` and include inline DOM/event logic | partial | medium | Source-contract tests pin globals and some page wiring | Bootstrap is explicit, but there is still view-side logic ownership |
| FM-07 | Frontend | `frontend/public/js/auth/modules/data-manager.js` | Raw `fetch` in a non-API module | API calls should go through dedicated API modules | `processLogin` and `processRegister` call `/users/*` directly from data-manager | partial | medium | Source-contract tests cover token handling, not module boundary | Acceptable adapter today, but still outside dedicated API modules |
| FM-08 | Frontend | `frontend/public/js/auth/register-controller.js` | Raw `fetch` to `/users/register` | Dedicated API modules; thin controllers | Controller performs direct network POST instead of calling API module | partial | medium | Source-contract tests cover behavior, not module boundary | Low-risk migration candidate |
| FM-09 | Frontend | `frontend/public/js/appointment/modules/server-data-loader.js` | Raw `fetch` for `/appointments/server-data` fallback | Dedicated API modules; boundary clarity | Loader fetches server data directly and mutates `window.serverData/currentUser/isAdmin` | partial | medium | `appointment-srp-split.test.js` pins current behavior | Adapter-like, but still direct fetch and global writes |
| FM-10 | Frontend | `frontend/public/js/appointment/modules/appointment-enricher.js` | Fallback fetch to `${apiBaseUrl}/patients/${id}` without `/api` prefix | Behavior contract; API boundary correctness | File documents a known bug: fallback fetch misses `/api`, causing 404 in practice | violation | high | `appointment-srp-split.test.js` intentionally preserves the bug as a source contract | Diagnostic finding only; future fix must be test-first |
| FM-11 | Frontend | `frontend/public/js/dashboard/dashboard-api.js` | Centralized dashboard API module | Dedicated API modules | `fetch` is centralized here, with normalization and error handling | aligned | high | Covered by `dashboard-api.test.js` | Good API boundary example |
| TM-01 | Tests | `backend/src/test/java/com/dh/dentalClinicMVC/controller/PatientControllerAuthzTest.java`, `DentistControllerAuthzTest.java`, `AppointmentControllerTest.java` | Security/authorization and object-level access tests | Behavior and boundary contracts; `@PreAuthorize`; ownership rules | Full security chain tests cover self-scope, admin access, and forbidden access | aligned | high | Strong existing coverage | Good protective tests to keep |
| TM-02 | Tests | `backend/src/test/java/com/dh/dentalClinicMVC/service/DashboardSnapshotServiceTest.java` | Service/use-case aggregation behavior | Service-level behavior contract | Pure unit-style test validates aggregation and fallback defaults | aligned | high | Direct service coverage exists | Good example of behavior-first test |
| TM-03 | Tests | `backend/src/test/java/com/dh/dentalClinicMVC/authentication/AuthenticationControllerTest.java` | Auth controller behavior with repository assertions | Behavior plus persistence side effect | Tests registration role rejection/defaulting and route removal | partial | medium | Covers register flows, not check-email delegation boundary | Useful but not targeted at the repo-bypass finding |
| TM-04 | Tests | `frontend/test/require-auth.test.js` | Route protection behavior | Frontend route behavior contract | Verifies unauthenticated redirects for protected routes and public-route access | aligned | high | Runtime route behavior covered | Good server-side protection evidence |
| TM-05 | Tests | `frontend/test/appointment-ui-manager-xss.test.js`, `patient-ui-manager-xss.test.js`, `dentist-ui-manager-xss.test.js` | jsdom/XSS-safe DOM rendering | XSS/safety behavior contracts | Verifies dangerous markup becomes inert text and delete handlers use closures | aligned | high | Runtime DOM safety coverage exists | Strong UI-safety example |
| TM-06 | Tests | `frontend/test/*-srp-split.test.js`, `frontend/test/global-wiring-source.test.js`, `frontend/test/client-token-handling.test.js` | Source-contract and window/global guards | Source-shape checks only when guarding explicit boundaries | Static source tests pin singleton wiring, token handling, and module splits | partial | high | Good boundary guards, but still source-shape heavy | Acceptable when paired with behavior tests |
| TM-07 | Tests | `frontend/test/app.test.js` | Skipped integration route tests | Behavior coverage gap | The basic route suite is skipped, so it does not enforce end-to-end page rendering | partial | high | No active runtime coverage from this file | Explicitly non-enforcing today |
| TM-08 | CI / Config | `.github/workflows/ci.yml`, `backend/pom.xml`, `frontend/package.json` | Backend + frontend test execution | CI/test enforcement; project test runners | CI runs `mvn -f backend/pom.xml test` and `npm test` under frontend | aligned | high | Both runners are wired in CI | Good enforcement baseline |
