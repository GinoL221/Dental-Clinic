// Archivo archivado: la versión activa del test fue movida a
// `frontend/public/js/debug-scripts/test-connection.js` para evitar servirlo en producción.
// Contenido original preservado en la carpeta debug-scripts.

// No-op exports para compatibilidad en caso de import accidental.
export async function testConnection() {
  return { status: 'archived' };
}

export async function cleanTestData() {
  return { status: 'archived' };
}
