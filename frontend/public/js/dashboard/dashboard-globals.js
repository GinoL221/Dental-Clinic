// dashboard-globals.js
// Este archivo solo inicializa las variables globales del dashboard si existen en el HTML
// El contenido real de las variables debe ser inyectado por el servidor en el EJS

// Este archivo no hace nada por sí solo, solo documenta la convención.
// La inicialización real sigue en el EJS para mantener los datos dinámicos.
// Toda la lógica y manipulación debe estar en dashboard-controller.js
